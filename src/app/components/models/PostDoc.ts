class PostDoc {
  id: number;
  title: string;
  body: string;
  styleBody: string;

  constructor(id: number, title: string, body: string, styleBody: string) {
    this.id = id;
    this.title = title;
    this.body = body;
    this.styleBody = styleBody;
  }
}
